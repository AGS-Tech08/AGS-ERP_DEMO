import { useEffect, useState } from 'react';
import { performanceService } from '../../services/performanceService';
export default function PerformanceList() { const [rows, setRows] = useState<any[]>([]); useEffect(() => { performanceService.list().then(response => setRows(response.data.data?.data ?? [])); }, []); return <section className="space-y-5"><h1 className="text-3xl font-bold">Performance</h1>{rows.length === 0 ? <p className="rounded bg-white p-6">No performance reviews found.</p> : <div className="overflow-x-auto rounded bg-white shadow"><table className="min-w-full"><thead><tr className="border-b text-left"><th className="p-3">Employee</th><th className="p-3">Period</th><th className="p-3">Overall Rating</th><th className="p-3">Status</th></tr></thead><tbody>{rows.map(row => <tr className="border-b" key={row.id}><td className="p-3">{row.employee?.user?.name ?? row.employee?.employee_id}</td><td className="p-3">{row.review_period}</td><td className="p-3">{row.overall_rating}</td><td className="p-3">{row.status}</td></tr>)}</tbody></table></div>}</section>; }