import { useState } from 'react';
import type { FormEvent } from 'react';
import { useNavigate } from 'react-router-dom';
import { taskService } from '../../services/taskService';
export default function TaskForm() { const navigate = useNavigate(); const [form, setForm] = useState({ task_title: '', task_description: '', priority: 'Medium', task_status: 'Not Started', completion_percentage: 0 }); const submit = async (e: FormEvent) => { e.preventDefault(); await taskService.create(form); navigate('/tasks'); }; return <form onSubmit={submit} className="max-w-2xl space-y-4 rounded bg-white p-6 shadow"><h1 className="text-2xl font-bold">Create Task</h1><input required className="w-full rounded border p-2" placeholder="Task title" value={form.task_title} onChange={e => setForm({ ...form, task_title: e.target.value })} /><textarea className="w-full rounded border p-2" placeholder="Description" value={form.task_description} onChange={e => setForm({ ...form, task_description: e.target.value })} /><select className="w-full rounded border p-2" value={form.priority} onChange={e => setForm({ ...form, priority: e.target.value })}><option>Low</option><option>Medium</option><option>High</option><option>Critical</option></select><button className="rounded bg-blue-600 px-4 py-2 text-white">Create Task</button></form>; }
